# Session Evidence Package

`GET /api/assemblies/{id}/expediente/package` → ZIP:

- Acta / Asistencia / Quórum / Votaciones (agregados) / Decisiones / Evidencias summary
- `Manifest.json` with per-file SHA-256
- **Video is never embedded** (download separately)

UI: `/expediente.html?assemblyId=`
