# Recording Retention

`PropertyRecordingPolicy.RetentionDays` (default 365) sets `RetentionUntilUtc` on recordings.

Automatic deletion is **not** enabled yet — architecture is ready for a future worker.
