# Result Visibility

Policies snapshotted on open:

- `HiddenUntilClose` — participation counts only until close
- `PresidentOnlyLive` — operators may see live trend
- `LiveResults` — assembly sees live trend

Unauthorized callers receive no partial trend payload from results API.
