# Eligibility

At open, accredited participants (not Registered/Left) are snapshotted into `VotingEligibilitySnapshots`.

Cast rejects voters missing from the snapshot when `EligibleVoters > 0`.

Representation: weights summed for the accredited representative — not double-counted as separate person+unit rows beyond configured reps.
