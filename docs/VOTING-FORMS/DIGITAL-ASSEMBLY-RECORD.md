# Digital Assembly Record

Routes:

- `/assemblies-history.html` — prior assemblies
- `/expediente.html?assemblyId=` — recording, downloads, timeline, ZIP package

ZIP includes manifest + SHA-256 checksums (video excluded by size; download separately when authorized).
