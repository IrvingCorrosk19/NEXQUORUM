# Co-ownership

Example: Unit 8B — Juan 60% + María 40%.

Quorum/voting weight uses **unit coefficient once** via unique active `AssemblyRepresentation(AssemblyId, UnitId)`.
SharePercent is ownership title, not double voting weight.
