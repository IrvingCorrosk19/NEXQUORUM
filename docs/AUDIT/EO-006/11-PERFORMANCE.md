# EO-006 — Performance

Search is client-filter over participant list (demo scale). Server-side search **NOT REQUIRED** for current 8-user demo.

Indexes: AssemblyId, UserId, UnitId, IsAccredited, unique active (AssemblyId, UnitId).
