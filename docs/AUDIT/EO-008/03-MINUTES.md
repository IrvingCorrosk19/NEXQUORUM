# EO-008 — Minutes

`GET /api/assemblies/{id}/minutes` → `AssemblyMinutesDocumentDto` with document id + SHA-256 of structured facts.

Print-first HTML via browser print (PDF = browser print). No AI narrative.
