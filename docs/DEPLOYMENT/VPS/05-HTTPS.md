# HTTPS

## Certificates

Let's Encrypt via Certbot (webroot `/var/www/certbot`) for:

- `asambleas.164.68.99.83.nip.io`
- `livekit-asambleas.164.68.99.83.nip.io`

Status: **HTTPS VALID** (auto-renew scheduled by Certbot).

## Notes

- Existing certificates for other VPS apps were not modified.
- Custom production DNS (non-nip.io) is optional later → see DOMAIN in certification report.
