# Voting Immutability

After first accepted ballot, critical fields cannot change on that session/motion.

Closed/cancelled sessions are historical evidence. Corrections require a new version, never silent rewrite.
