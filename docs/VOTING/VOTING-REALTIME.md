# Voting Realtime (live workspace)

Events:

- `votingOpened` / `voteTallyUpdated` / `votingClosed` (existing)
- `votingCancelled` — owners see annulment + reason
- `votingVersionCreated` — new draft version available
- `motionUpdated` — admin draft changes (owners only see after present/open)
